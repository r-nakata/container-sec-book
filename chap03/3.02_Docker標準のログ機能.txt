# 3.02 Docker標準のログ機能
# 『コンテナセキュリティ』サンプルコード

# コンテナのログを表示
docker logs container_name

# リアルタイムでログを監視
docker logs -f container_name

# 最新の100行のみ表示
docker logs --tail 100 container_name

# 特定の時間以降のログを表示
docker logs --since "2025-06-24T10:00:00" container_name
